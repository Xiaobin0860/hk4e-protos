syntax = "proto3";

option go_package = "./;proto";

import "Reliquary.proto";
import "Weapon.proto";

message Equip {
	oneof detail {
		Reliquary reliquary = 1;
		Weapon weapon = 2;
	}
	bool isLocked = 3;
}