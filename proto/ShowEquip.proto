syntax = "proto3";

option go_package = "./;proto";

import "Reliquary.proto";
import "Weapon.proto";

message ShowEquip {
	uint32 itemId = 1;
	oneof detail {
		Reliquary reliquary = 2;
		Weapon weapon = 3;
	}
}